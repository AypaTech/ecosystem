<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_telegram_update_list" model="ir.ui.view">
        <field name="name">telegram.update.list</field>
        <field name="model">telegram.update</field>
        <field name="arch" type="xml">
            <list create="0" edit="0" decoration-danger="state == 'error'" decoration-muted="state == 'ignored'">
                <header>
                    <button name="action_reprocess" type="object" string="Reprocess"/>
                </header>
                <field name="received_at"/>
                <field name="bot_id"/>
                <field name="update_id"/>
                <field name="update_type"/>
                <field name="source" optional="show"/>
                <field name="attempts" optional="hide"/>
                <field name="error" optional="show"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_telegram_update_form" model="ir.ui.view">
        <field name="name">telegram.update.form</field>
        <field name="model">telegram.update</field>
        <field name="arch" type="xml">
            <form create="0" edit="0">
                <header>
                    <button name="action_reprocess" type="object" string="Reprocess" invisible="state not in ('error', 'ignored')"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="bot_id"/>
                            <field name="update_id"/>
                            <field name="update_type"/>
                            <field name="source"/>
                        </group>
                        <group>
                            <field name="received_at"/>
                            <field name="processed_at"/>
                            <field name="attempts"/>
                            <field name="payload_hash"/>
                        </group>
                    </group>
                    <field name="error" invisible="not error"/>
                    <field name="payload" groups="base.group_system"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_telegram_update_search" model="ir.ui.view">
        <field name="name">telegram.update.search</field>
        <field name="model">telegram.update</field>
        <field name="arch" type="xml">
            <search>
                <field name="bot_id"/>
                <field name="update_id"/>
                <filter name="filter_pending" string="Pending" domain="[('state', '=', 'pending')]"/>
                <filter name="filter_error" string="Errors" domain="[('state', '=', 'error')]"/>
            </search>
        </field>
    </record>

    <record id="action_telegram_update" model="ir.actions.act_window">
        <field name="name">Inbound Updates</field>
        <field name="res_model">telegram.update</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
